import LoadingAnimation from "./LoadingAnimation";

export default function Loading() {
  // Use the existing LoadingAnimation component for a consistent branded experience
  return <LoadingAnimation />;
}
